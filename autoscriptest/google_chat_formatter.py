from autoscriptest.models import GoogleChatMessage

def format_google_chat_message(google_chat_message: GoogleChatMessage) -> str:
    """Formats a Google Chat message using Markdown syntax"""
    # Use Markdown syntax to format the message
    formatted_text = f"